# Requirements for AgentCore CLI deployment path.
# Used by: agentcore configure --requirements-file infra/aws/agentcore/requirements.txt
#
# The toolkit-generated Dockerfile installs THIS file (it does not `pip install`
# the ad_seller package), so every runtime import dependency must be listed
# here — including the git-based shared contract library — or the container
# fails at import with ModuleNotFoundError. Kept in sync with pyproject.toml.
#
# NOTE: the build image (astral uv slim) has no `git` binary, so a
# `git+https://` source fails with "Git executable not found". Use the GitHub
# archive tarball for iab-agentic-primitives — a plain HTTPS download that
# needs no git. Keep it pinned to the same tag as pyproject.toml (v0.5.1).
bedrock-agentcore
crewai[anthropic]>=1.14.0
crewai-tools[mcp]>=1.14.0
# Shared IAB agentic contract catalog (Product/QuoteRequest/DealBooking/registry
# primitives). The crew path and API routers import iab_agentic_primitives, so
# it must be in the AgentCore image. Pinned to the git-free GitHub archive
# tarball because the CodeBuild image has no git (a git+https dep fails to
# install). Keep in sync with the buyer agent's pin.
iab-agentic-primitives[client] @ https://github.com/IABTechLab/iab-agentic-primitives/archive/refs/tags/v0.5.1.tar.gz
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
mcp>=1.0.0
python-dotenv>=1.0.0
typer>=0.12.0
rich>=13.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
aiosqlite>=0.20.0
asyncpg>=0.30.0
redis>=5.0.0
iab-agentic-primitives[client] @ https://github.com/IABTechLab/iab-agentic-primitives/archive/refs/tags/v0.5.1.tar.gz
